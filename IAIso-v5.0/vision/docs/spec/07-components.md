# Section 03: Component Structure
IAIso v5.0 is modular and versioned. Every artifact is a JSON component.
- **ID**: Unique versioned identifier.
- **ConsentScope**: Authorization tag (e.g., `gov.layer2.planning`).
- **Implementation**: Path to prompt templates and runtime logic.
